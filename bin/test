#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "dotenv"
Dotenv.load("../.env")
require "soldoni"

tito = Tito.new
return unless tito.should_send_daily_message?

puts tito.daily_message
