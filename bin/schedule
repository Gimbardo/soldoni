#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "dotenv"
Dotenv.load("../.env")
require "soldoni"
require "rufus-scheduler"
require "tzinfo"

scheduler = Rufus::Scheduler.new

scheduler.cron "40 23 * * *" do
  puts "[#{Time.now}] Calcolando i 💰💰💰 SOLDONI 💰💰💰 ..."

  Soldoni.fetch_and_notify
end

scheduler.join
